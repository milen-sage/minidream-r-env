version: '3'
services:

  rstudio:
    environment:
      - PASSWORD=minidream
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./home:/home
    ports:
      - "8787:8787"

